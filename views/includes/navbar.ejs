
<style>
      .search-btn{
        display: inline;
        background-color:#fe424d ;
        color: #fff;
        border-radius: 25px;
        padding: 0 1rem 0rem 1rem;
      }
      .search-btn:hover{
        display: inline;
        background-color:#fe424d ;
        color: #fff;
      }
      .search-btn i{
        display: inline;
        margin-right: 0.5rem;
      }

      .search-input{
        border-radius: 25px;
        padding: 0.5rem 3rem 0.5rem 3rem ;
        font-size: 1rem;
      }
      .navbar .search-input {
  width: 100%;
  margin-bottom: 0.5rem;
}

.navbar-toggler {
  border: none;
  outline: none;
}

@media (max-width: 768px) {
  .navbar-nav {
    text-align: center;
  }
}

/* Set white background for dropdown menu 
.navbar-collapse {
  /* background-color: white; White background */
 /* border-radius: 0.25rem; /* Optional rounded corners */
 /* box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1); /* Optional shadow for better visibility */
 /* padding: 1rem;  Optional padding for spacing */
 /* }*/

/* Center-align links in smaller viewports */
@media (max-width: 768px) {
  .navbar-collapse {
    text-align: center;
  }

  .navbar-nav .nav-link {
    margin: 0.5rem 0; /* Add spacing between links */
  }
}
</style>

<nav class="navbar navbar-expand-md bg-body-light border-bottom sticky-top" >
  <div class="container-fluid">
    <a class="navbar-brand" href="/listings"><i class="fa-regular fa-compass"></i></a>
    <button
      class="navbar-toggler"
      type="button"
      data-bs-toggle="collapse"
      data-bs-target="#navbarNavAltMarkup"
       >
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
      <div class="navbar-nav">

          <a class="nav-link" href="/listings">Explore</a>


      </div>
      <div class="navbar-nav ms-auto">
        <form class="d-flex" role="search" action="/listings/search">
          <input class="form-control me-2 search-input" type="search" placeholder="Search destination" aria-label="Search" name="search">
          <button class="btn search-btn" type="submit">
            <i class="fa-solid fa-magnifying-glass"></i>Search</button>
        </form>
      </div>
      <div class="navbar-nav ms-auto">
        <a class="nav-link" href="/listings/new">Airbnd your home</a>
        <% if(!currUser) {%>
        <a class="nav-link " href="/signup"><b>Sign up</b></a>
        <a class="nav-link" href="/login"><b>Log in</b></a>
        <% }%>
        <% if(currUser) {%>
        <a class="nav-link" href="/logout"><b>Log out</b></a>
        <% }%>

      </div>
    </div>
  </div>
</nav>
